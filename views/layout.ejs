<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= site.tagline %>">
    <meta name="robots" content="noindex, nofollow">
    
    <!-- Age Verification -->
    <meta name="rating" content="adult">
    <meta name="adult-content" content="yes">
    
    <title><%= title %> - <%= site.name %></title>
    
    <!-- Preload fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Tailwind CSS -->
    <link rel="stylesheet" href="/css/output.css">
    
    <!-- Favicon -->
    <link rel="icon" href="/images/favicon.ico">
</head>
<body class="antialiased">
    
    <!-- Age Verification Modal -->
    <div id="ageModal" class="fixed inset-0 z-50 hidden">
        <div class="absolute inset-0 bg-black/95 backdrop-blur-sm"></div>
        <div class="relative h-full flex items-center justify-center p-4">
            <div class="glass rounded-3xl p-8 md:p-12 max-w-md w-full text-center animate-fade-in">
                <div class="mb-6">
                    <svg class="w-20 h-20 mx-auto text-primary-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                    </svg>
                </div>
                <h2 class="text-3xl font-display font-bold mb-4">Age Verification</h2>
                <p class="text-gray-300 mb-8">
                    This website contains adult content. You must be 18+ to enter.
                </p>
                <div class="space-y-3">
                    <button onclick="confirmAge()" class="btn-primary w-full">
                        I am 18 or older - Enter
                    </button>
                    <button onclick="denyAge()" class="btn-secondary w-full">
                        I am under 18 - Exit
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Navigation -->
    <%- include('partials/nav') %>
    
    <!-- Main Content -->
    <main class="min-h-screen">
        <%- body %>
    </main>
    
    <!-- Footer -->
    <%- include('partials/footer') %>
    
    <!-- Scripts -->
    <script src="/js/main.js"></script>
    
</body>
</html>
